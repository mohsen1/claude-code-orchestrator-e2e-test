# Database Configuration
# SQLite database file path (relative to project root or absolute path)
DATABASE_URL="file:./dev.db"

# NextAuth Configuration
# The URL of your application (no trailing slash)
NEXTAUTH_URL="http://localhost:3000"

# Secret key for NextAuth.js (generate with: openssl rand -base64 32)
NEXTAUTH_SECRET="your-secret-here-generate-with-openssl-rand-base64-32"

# Application Environment
NODE_ENV="development"

# OAuth Providers - Google (Optional)
# Get credentials from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# Session Configuration
# Session lifetime in seconds (default: 30 days)
NEXTAUTH_SESSION_MAX_AGE="2592000"

# Database Encryption (Optional but recommended for production)
# ENCRYPTION_KEY="your-32-character-encryption-key"

# WebSocket/Socket.io Configuration
# Socket.io port (if running separately from Next.js)
SOCKET_PORT="3001"

# Logging Configuration
# Log level: 'debug', 'info', 'warn', 'error'
LOG_LEVEL="info"

# Feature Flags
ENABLE_EMAIL_AUTH="true"
ENABLE_OAUTH_AUTH="true"
ENABLE_REALTIME="true"

# Backup Configuration (for production)
BACKUP_ENABLED="false"
BACKUP_SCHEDULE="0 2 * * *" # Cron expression for daily 2 AM backups
BACKUP_RETENTION_DAYS="30"
