version: '3.9'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: splitsync-app
    restart: unless-stopped
    ports:
      - '3000:3000'
    environment:
      - NODE_ENV=production
      - DATABASE_URL=file:./data/prod.db
      - NEXTAUTH_URL=${NEXTAUTH_URL:-http://localhost:3000}
      - NEXTAUTH_SECRET=${NEXTAUTH_SECRET}
    volumes:
      - ./data:/app/data
    healthcheck:
      test: ['CMD', 'wget', '--quiet', '--tries=1', '--spider', 'http://localhost:3000/api/health']
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    networks:
      - splitsync-network

  # Optional: PostgreSQL for production (replace SQLite)
  # postgres:
  #   image: postgres:16-alpine
  #   container_name: splitsync-postgres
  #   restart: unless-stopped
  #   environment:
  #     - POSTGRES_USER=${POSTGRES_USER:-splitsync}
  #     - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
  #     - POSTGRES_DB=${POSTGRES_DB:-splitsync}
  #   volumes:
  #     - postgres-data:/var/lib/postgresql/data
  #   ports:
  #     - '5432:5432'
  #   healthcheck:
  #     test: ['CMD-SHELL', 'pg_isready -U ${POSTGRES_USER:-splitsync}']
  #     interval: 10s
  #     timeout: 5s
  #     retries: 5
  #   networks:
  #     - splitsync-network

  # Optional: Redis for session storage and caching
  # redis:
  #   image: redis:7-alpine
  #   container_name: splitsync-redis
  #   restart: unless-stopped
  #   command: redis-server --appendonly yes
  #   volumes:
  #     - redis-data:/data
  #   ports:
  #     - '6379:6379'
  #   healthcheck:
  #     test: ['CMD', 'redis-cli', 'ping']
  #     interval: 10s
  #     timeout: 5s
  #     retries: 5
  #   networks:
  #     - splitsync-network

networks:
  splitsync-network:
    driver: bridge

volumes:
  postgres-data:
  redis-data:
