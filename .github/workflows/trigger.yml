name: Orchestrator Trigger

on:
  issues:
    types: [labeled]

permissions:
  contents: write
  actions: write

jobs:
  trigger:
    if: github.event.label.name == 'orchestrator'
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Director
        run: |
          echo "Triggering director workflow for issue ${{ github.event.issue.number }}"
