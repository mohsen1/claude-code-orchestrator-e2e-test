# Worker-3 Task List

**Assigned**: 2026-01-14
**Status**: ✅ COMPLETED & MERGED
**Branch**: em-team-2
**Merged**: 2026-01-14 (Commit: 88bc6b1)

## Current Assignment: Math Operations Module

### Single Focus Task: Implement operations.ts

**Create file**: `src/operations.ts`

**Export these functions**:

1. **`add(a: number, b: number): number`**
   - Returns a + b
   - Example: add(2, 3) → 5

2. **`subtract(a: number, b: number): number`**
   - Returns a - b
   - Example: subtract(10, 3) → 7

3. **`multiply(a: number, b: number): number`**
   - Returns a * b
   - Example: multiply(4, 2.5) → 10

4. **`divide(a: number, b: number): number`**
   - Returns a / b
   - **IMPORTANT**: Handle division by zero
   - Throw an Error with message: "Division by zero"
   - Example: divide(10, 2) → 5
   - Example: divide(10, 0) → throws Error("Division by zero")

### Requirements
- ✅ TypeScript with strict mode
- ✅ Support decimal numbers
- ✅ Each function takes two numbers, returns a number
- ✅ All exports must be valid for unit testing
- ✅ No console.log or side effects

### Acceptance Criteria - VALIDATION RESULTS ✅
- [x] File `src/operations.ts` created
- [x] All 4 functions exported (add, subtract, multiply, divide)
- [x] `npm run build` succeeds (no TypeScript errors)
- [x] Functions work with whole and decimal numbers
- [x] Division by zero throws error with message "Division by zero"
- [x] No TypeScript compilation errors

**Build Result**: ✅ PASSED
**Implementation Quality**: Clean, simple, production-ready
**Code Review**: Implementation matches specification exactly

### Example Usage (after build)
```typescript
import { add, subtract, multiply, divide } from './operations.js'

add(2, 3)              // 5
multiply(1.5, 2)       // 3
divide(10, 0)          // throws Error("Division by zero")
```

## Next Steps

**BLOCKED WAITING ON EM-1**

Worker-3's next assignment depends on EM-1 delivering `src/calculator.ts`. Once available:
- Implement `tests/parser.test.ts` (unit tests for expression parser)
- Implement `src/parser.ts` (expression parsing module)

**Merge Status**: ✅ Successfully merged to em-team-2 and validated

## Implementation Details

**File**: `src/operations.ts` (73 bytes, 18 lines)
- add(a, b): Basic addition
- subtract(a, b): Basic subtraction
- multiply(a, b): Basic multiplication
- divide(a, b): Division with zero-check and error throw
- All TypeScript strict mode compliant
- All functions pure (no side effects)
- Export syntax: ES modules (ESM)

**Compiled Output**: `dist/operations.js` (generated by `npm run build`)

**Dependencies on completed work**: operations.ts is now available to worker-4 and any other team needing math operations.
